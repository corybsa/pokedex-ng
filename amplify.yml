version: 1
applications:
  frontend:
    phases:
      build:
        commands:
          - npm cache clean
          - npm install
          - npm run build
    artifacts:
      baseDirectory: dist/pokedex
      files:
        - '**/*'
    cache:
      paths:
        - node_modules/**/*
